# IosTest

Swift로 Alamofire 통신을 연습하기 위한 간단한 샘플입니다. `NetworkClient`가 `/todos/{id}` 엔드포인트를 호출한다고 가정하고, `URLProtocol` 기반 스텁으로 응답을 주입해 예제 호출 흐름을 확인합니다.

## 실행 방법
1. 스텁을 사용한 예제 실행
   ```bash
   swift run
   ```
